var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#The-search","page":"API reference","text":"","title":"The search"},{"category":"section","location":"api/#The-unknown-vector","page":"API reference","text":"","title":"The unknown vector"},{"category":"section","location":"api/#Matrix-free-linearisation","page":"API reference","text":"","title":"Matrix-free linearisation"},{"category":"section","location":"api/#Saving-and-loading-orbits","page":"API reference","text":"","title":"Saving and loading orbits"},{"category":"section","location":"api/#Sensitivity-analysis-(experimental)","page":"API reference","text":"","title":"Sensitivity analysis (experimental)"},{"category":"function","location":"api/#NKSearch.search!","page":"API reference","text":"search!(G, L, S, F, dS, z0::MVector{X,N,2}, opts=Options()) -> status\nsearch!(G, L,       F,     z0::MVector{X,N,1}, opts=Options()) -> status\n\nRefine the candidate orbit z0 in place with a Newton–Krylov / multiple- shooting iteration until convergence or opts.maxiter is reached.\n\nUse the 6-argument form to search for a relative periodic orbit (an orbit closing up to a spatial shift, z0 has a shift unknown, NS == 2), and the 4-argument form for an ordinary periodic orbit (NS == 1).\n\nz0 is overwritten with the refined orbit. The return value is a status symbol such as :converged, :maxiter_reached, :min_step_reached, or :callback_satisfied (line-search method returns nothing).\n\nArguments\n\nG: nonlinear flow operator. G(x, (0, T)) advances state x in place over time span (0, T).\nL: linearised flow operator. L(Flows.couple(x, y), (0, T)) advances the base state x and the perturbation y in place. To avoid writing a hand-coded linearisation, pass a JFOp built from G.\nS: spatial shift operator (relative periodic orbits only). S(x, s) shifts state x by s in place.\nF: right-hand side of the governing ODE. F(out, x) overwrites out with the time derivative at x; it sets the phase-locking constraint that removes the time-translation degeneracy.\ndS: generator of the spatial shift (relative periodic orbits only). dS(out, x) overwrites out, fixing the spatial phase.\nz0::MVector: initial guess; overwritten with the result. See MVector.\nopts::Options: solver settings; see Options.\n\nG and L are deep-copied once per shooting segment, so the same operator instance can be passed for all segments.\n\nnote: Threading\nThe iterative methods (:ls_iterative, :tr_iterative) parallelise the shooting segments across tasks; run Julia with as many threads as there are segments. The direct methods (:ls_direct, :tr_direct) require a single thread.\n\nExample\n\nusing NKSearch, Flows, LinearAlgebra\n\nF = ...   # ODE right-hand side, callable as F(t, x, dxdt)\nG = flow(F, RK4(zeros(2)), TimeStepConstant(1e-3))                      # nonlinear flow\nL = flow(couple(F, Fjac), RK4(couple(zeros(2), zeros(2))), TimeStepConstant(1e-3))\n\nz = MVector(([2.0, 0.0], [-2.0, 0.0]), 2π)   # 2-segment guess, period 2π\nsearch!(G, L, (dxdt, x) -> F(0, x, dxdt), z,\n        Options(method=:tr_iterative, maxiter=25))\n\nSee the manual for a complete, runnable tutorial.\n\n\n\n\n\n","title":"NKSearch.search!"},{"category":"type","location":"api/#NKSearch.Options","page":"API reference","text":"Options(; kwargs...)\n\nConfiguration for search!. All fields are keyword arguments with defaults; only override what you need.\n\nKey options\n\nmethod::Symbol = :ls_direct: globalization + linear-solve strategy, one of :ls_direct, :ls_iterative, :tr_direct, :tr_iterative. The ls_ variants use a line search, the tr_ variants a trust region (dogleg for :tr_direct, hookstep for :tr_iterative). The _direct variants assemble and LU-factorise the Jacobian; the _iterative variants solve it matrix-free with GMRES. See the manual for guidance.\nmaxiter::Int = 10: maximum number of Newton iterations.\ne_norm_tol::Float64 = 1e-10: convergence tolerance on the residual norm.\ndz_norm_tol::Float64 = 1e-10: convergence tolerance on the Newton step norm.\nϵ::Float64 = 1e-6: step used for the finite-difference approximation of the time derivative of the flow operator.\nfd_order::Int = 2: order (1 or 2) of that finite-difference scheme.\nverbose::Bool = true, io = stdout, skipiter::Int = 1: control status printing (io receives a table, every skipiter iterations).\ncallback = (iter, z) -> false: called after each iteration; returning true terminates the search.\n\nLine-search options\n\nls_maxiter::Int = 10, ls_rho::Float64 = 0.5: maximum backtracking iterations and step-reduction factor.\n\nGMRES options (iterative methods)\n\ngmres_maxiter::Int = 10, gmres_rtol::Float64 = 1e-3, gmres_verbose::Bool = true, gmres_callback = nothing, gmres_start = dz -> (dz .*= 0; dz): GMRES iteration count, relative tolerance, verbosity, callback, and warm-start initialiser.\n\nTrust-region options (tr_ methods)\n\ntr_radius_init::Float64 = 1, tr_radius_max::Float64 = 1e8: initial and maximum trust-region radius.\nmin_step::Float64 = 1e-4: minimum accepted step before stopping.\nNR_lim::Float64 = 1e-8: residual level below which a full Newton step is taken regardless of the trust-region test.\nα::Float64 = 1, eta::Float64 = 0.0: over-relaxation factor and the minimum reduction ratio for accepting a step.\n\nExample\n\nopts = Options(method=:tr_iterative, maxiter=25,\n               e_norm_tol=1e-12, gmres_maxiter=5, verbose=false)\n\n\n\n\n\n","title":"NKSearch.Options"},{"category":"type","location":"api/#NKSearch.MVector","page":"API reference","text":"MVector(x::NTuple{N,X}, T::Real)            -> MVector{X,N,1}\nMVector(x::NTuple{N,X}, T::Real, s::Real)   -> MVector{X,N,2}\n\nA multiple-shooting unknown: the state of a candidate (relative) periodic orbit, packaged as a single vector for the Newton solver.\n\nIt bundles N seed states x = (x₁, …, x_N) sampled along the orbit with the scalar unknowns d: the period T and, for a relative periodic orbit, an additional spatial shift s.\n\nArguments\n\nx::NTuple{N,X}: the N seeds, one per shooting segment. Each seed is a state of type X (see the element-type requirements below).\nT::Real: the orbit period (the first scalar unknown).\ns::Real: optional spatial shift, present only for relative periodic orbits with a continuous spatial symmetry.\n\nElement type X\n\nX must support LinearAlgebra.dot, Base.similar, Base.zero, and full broadcasting against other X values and scalars. Plain Vector{Float64} satisfies this, as do the field types in Flows.\n\nFields\n\nx::NTuple{N,X}: the seeds.\nd::NTuple{NS,Float64}: the scalar unknowns, (T,) or (T, s).\n\nMVector is itself an AbstractVector{Float64} of length N*length(x₁) + NS, so it can be passed to linear-algebra routines; use tovector / fromvector! to convert to and from a flat Vector{Float64}.\n\nExamples\n\njulia> z = MVector(([1.0, 2.0], [3.0, 4.0]), 6.28);\n\njulia> nsegments(z)\n2\n\njulia> z.d\n(6.28,)\n\njulia> tovector(z)\n5-element Vector{Float64}:\n 1.0\n 2.0\n 3.0\n 4.0\n 6.28\n\n\n\n\n\n","title":"NKSearch.MVector"},{"category":"function","location":"api/#NKSearch.nsegments","page":"API reference","text":"nsegments(z::MVector) -> Int\n\nReturn the number of multiple-shooting segments (seeds) N stored in z.\n\n\n\n\n\n","title":"NKSearch.nsegments"},{"category":"function","location":"api/#NKSearch.tovector","page":"API reference","text":"tovector(z::MVector) -> Vector{Float64}\n\nFlatten z into a freshly allocated Vector{Float64} of length N*length(z[1]) + NS: the N seeds concatenated first, followed by the scalar unknowns z.d (period and optional shift). Inverse of fromvector!.\n\n\n\n\n\n","title":"NKSearch.tovector"},{"category":"function","location":"api/#NKSearch.fromvector!","page":"API reference","text":"fromvector!(out::MVector, v::Vector{<:Real}) -> out\n\nCopy the flat representation v (as produced by tovector) back into the seeds and scalar unknowns of out, in place, and return out. v must have length nsegments(out)*length(out[1]) + NS.\n\n\n\n\n\n","title":"NKSearch.fromvector!"},{"category":"function","location":"api/#NKSearch.find_number_of_segments","page":"API reference","text":"find_number_of_segments(M, T, Tmin, Tmax) -> Int\n\nPick a number of shooting segments N that divides M evenly while keeping each segment's duration T/N within [Tmin, Tmax]. Concretely, return the first integer N with M % N == 0 in the range ceil(T/Tmax) ≤ N ≤ floor(T/Tmin), or 0 if none exists.\n\nThis is useful when the seeds come from a stored trajectory of M samples that must be split into equal segments.\n\nArguments\n\nM::Int: number of available samples / time steps along the trajectory.\nT::Real: total period.\nTmin::Real, Tmax::Real: minimum and maximum allowed duration per segment (Tmin < Tmax).\n\nExamples\n\njulia> find_number_of_segments(12, 10.0, 1.0, 3.0)\n4\n\n\n\n\n\n","title":"NKSearch.find_number_of_segments"},{"category":"type","location":"api/#NKSearch.JFOp","page":"API reference","text":"JFOp(G, x, epsilon::Float64=1e-6) -> op\n\nA matrix-free (\"Jacobian-free\") linearised flow operator, suitable as the L argument of search! when no hand-coded linearisation is available.\n\nIt approximates the action of the linearised flow on a perturbation y with a finite-difference quotient of the nonlinear flow G:\n\nLx y approx fracG(x + epsilon y) - G(x)epsilon\n\nArguments\n\nG: nonlinear flow operator, callable as G(x, span) (in place).\nx: a sample state, used only to allocate internal temporaries (similar(x)).\nepsilon::Float64: finite-difference step.\n\nUsage\n\nCall it like the exact linearised operator, op(Flows.couple(x, y), (0, T)), which advances base state x and perturbation y in place. The base trajectory G(x, span) is cached (keyed by span and x) so it is not recomputed for repeated perturbations about the same point.\n\nwarning: Warning\nThe quotient is a first-order forward difference regardless of Options.fd_order, and x must be real-valued.\n\n\n\n\n\n","title":"NKSearch.JFOp"},{"category":"function","location":"api/#NKSearch.save_seeds","page":"API reference","text":"save_seeds(z::MVector, path::String, other=Dict{String,Any}())\n\nWrite the orbit z to the HDF5 file path. Each seed is stored as a dataset (seed_i, or seed_i_real/seed_i_imag for complex states), the scalar unknowns z.d as attributes d1, d2, …, and every key/value in other as an attribute prefixed with other_.\n\nThe seeds are written via parent(z.x[i]), so this supports states that wrap an array (e.g. Flows field types). Use load_seeds! to read the file back.\n\n\n\n\n\n","title":"NKSearch.save_seeds"},{"category":"function","location":"api/#NKSearch.load_seeds!","page":"API reference","text":"load_seeds!(fun, path::String) -> (z::MVector, other::Dict{String,Any})\n\nRead an orbit written by save_seeds from the HDF5 file path.\n\nfun is applied to each raw seed array as it is read, so it can wrap the plain array back into the state type you use (pass identity to keep plain arrays). Returns the reconstructed MVector together with a Dict of any extra attributes that were stored under the other_ prefix.\n\n\n\n\n\n","title":"NKSearch.load_seeds!"},{"category":"function","location":"api/#NKSearch.make_adjoint_problem","page":"API reference","text":"make_adjoint_problem(z, store, L, J, S, D, jTJ) -> (A, rhs)\nmake_adjoint_problem(z, store, L, J,    D, jTJ) -> (A, rhs)   # no shift\n\nAssemble the linear system for the adjoint sensitivity of a converged (relative) periodic orbit z. Returns a matrix-free operator A (callable as A * w and usable with GMRES) together with its right-hand side rhs.\n\nArguments\n\nz::MVector: a converged orbit (see search!).\nstore: a thread-safe trajectory cache that returns the orbit state and its time derivative at a requested time, callable as store(out, t, Val(k)) for derivative order k.\nL: homogeneous adjoint flow operator.\nJ: inhomogeneous (forcing) adjoint operator.\nS, D: spatial-shift operator and the time/space derivative operators, as for search! (S omitted in the no-shift form).\njTJ::Real: the cost-gradient scalar placed in the bottom rows of rhs.\n\nwarning: Experimental\nThe sensitivity interface is still being stabilised; argument conventions (especially the store and adjoint-operator contracts) may change, and the no-shift path is not yet covered by tests. The companion tangent problem is built by the (currently unexported) NKSearch.make_tangent_problem.\n\n\n\n\n\n","title":"NKSearch.make_adjoint_problem"},{"category":"section","location":"concepts/#Concepts","page":"Concepts","text":"This page explains the ideas you need to set up a search. None of it requires reading the source.","title":"Concepts"},{"category":"section","location":"concepts/#Multiple-shooting","page":"Concepts","text":"A naive formulation represents a periodic orbit by a single point x_0 and asks for G(x_0 T) = x_0. For long or strongly unstable orbits this is badly conditioned: a small change in x_0 can produce a huge change in G(x_0 T).\n\nMultiple shooting splits the orbit into N segments and stores one seed state per segment, x_1 dots x_N. Each seed is integrated only over a fraction TN of the period, and the unknowns are tied together by matching conditions: the end of segment i must equal the start of segment i+1 (with the last segment wrapping back to the first, up to a shift for a relative periodic orbit). Shorter segments mean shorter integrations and a better-conditioned linear system.","title":"Multiple shooting"},{"category":"section","location":"concepts/#The-MVector-unknown","page":"Concepts","text":"All of the unknowns are packed into a single MVector: the N seeds plus the scalars (period T, and optionally a shift s).\n\nz = MVector((x1, x2, x3), T)        # periodic orbit:          3 seeds + period\nz = MVector((x1, x2, x3), T, s)     # relative periodic orbit: 3 seeds + period + shift\n\nThe seeds may be plain Vectors or any state type that supports dot, similar, zero, and broadcasting (for example the field types in Flows). MVector behaves as an AbstractVector{Float64}, and tovector / fromvector! convert to and from a flat Vector{Float64} when needed.\n\njulia> z = MVector(([1.0, 2.0], [3.0, 4.0]), 6.28);\n\njulia> nsegments(z), z.d\n(2, (6.28,))\n\njulia> tovector(z)\n5-element Vector{Float64}:\n 1.0\n 2.0\n 3.0\n 4.0\n 6.28\n\nIf your seeds come from a stored trajectory of M samples, use find_number_of_segments to pick an N that divides M evenly while keeping each segment within a target duration.","title":"The MVector unknown"},{"category":"section","location":"concepts/#Phase-conditions","page":"Concepts","text":"A periodic orbit has no preferred starting time: shifting the whole orbit along itself gives another valid solution. A relative periodic orbit additionally has no preferred spatial phase. These degeneracies would make the Newton system singular, so they are removed by phase-locking constraints:\n\nthe time phase is fixed using the right-hand side f of the ODE — the F argument to search!, with the convention F(out, x);\nthe spatial phase (relative periodic orbits only) is fixed using the shift generator — the dS argument, with the convention dS(out, x).\n\nYou do not implement the constraints themselves; you only provide F (and dS), and NKSearch builds the constraints from them.","title":"Phase conditions"},{"category":"section","location":"concepts/#The-operators-you-provide","page":"Concepts","text":"search! is matrix-free: instead of a Jacobian, you pass operators that act on states.\n\nOperator Role Call signature\nG nonlinear flow G(x, (0, T)) advances x in place\nL linearised flow L(couple(x, y), (0, T)) advances base x and perturbation y\nF ODE right-hand side F(out, x) writes the time derivative\nS spatial shift (RPO only) S(x, s) shifts x by s\ndS shift generator (RPO only) dS(out, x) writes the spatial phase direction\n\nG and L typically come from Flows (flow(...) and flow(couple(...))). If you do not have a hand-coded linearisation L, wrap G in a JFOp, which approximates L with a finite difference of G:\n\nL = JFOp(G, similar_state)   # matrix-free, finite-difference linearisation","title":"The operators you provide"},{"category":"section","location":"concepts/#Indexing-and-layout-conventions","page":"Concepts","text":"Seeds are ordered along the orbit: z[i] is the i-th seed, segment i spans time ((i-1)·T/N, i·T/N), and segment N connects back to segment 1.\nThe scalar unknowns are z.d: z.d[1] is the period T, and z.d[2] is the shift s when present (nsegments counts seeds, not scalars).\nIn the flat layout from tovector, the N seeds come first (concatenated) and the scalars last.","title":"Indexing and layout conventions"},{"category":"section","location":"concepts/#Common-pitfalls","page":"Concepts","text":"Wrong F signature. search! expects F(out, x). If your right-hand side is f(t, x, dxdt) (the Flows convention), wrap it: (out, x) -> f(0, x, out).\nThreads vs. method. The _iterative methods parallelise the segments across tasks and are intended to run with one thread per segment; the _direct methods must run single-threaded (they will error otherwise). See Solver methods.\nGuess too far from a real orbit. Newton converges locally. Start from a reasonable guess (e.g. a recurrence detected in a long trajectory) and prefer a trust-region method far from the solution.\nPeriod sign. A line/trust step can momentarily drive T negative; the solvers guard against the resulting invalid time spans, but a guess with a realistic period helps avoid wasted iterations.","title":"Common pitfalls"},{"category":"section","location":"methods/#Solver-methods","page":"Solver methods","text":"search! supports four methods, selected with Options(method = …). They differ in two independent choices: how the Newton step is globalized (line search vs. trust region) and how the linear system is solved (direct LU factorisation vs. matrix-free GMRES).\n\nmethod Globalization Linear solve Threads\n:ls_direct (default) line search LU factorisation single\n:ls_iterative line search GMRES (matrix-free) one per segment\n:tr_direct trust region (dogleg) LU factorisation single\n:tr_iterative trust region (hookstep) GMRES (matrix-free) one per segment","title":"Solver methods"},{"category":"section","location":"methods/#Choosing-a-linear-solve","page":"Solver methods","text":"Direct (_direct). The Jacobian is assembled and LU-factorised. This is simplest and exact, but forming the matrix costs O(n) flow linearisations per segment (where n is the state size), so it is only practical for small states. Direct methods run single-threaded and will raise an error if Julia is started with more than one thread.\nIterative (_iterative). The Jacobian is never formed; GMRES uses only matrix–vector products, each one a linearised flow. This scales to large states (discretised PDEs) and parallelises the shooting segments across tasks. Run Julia with as many threads as segments. Tune the solve with gmres_maxiter and gmres_rtol.","title":"Choosing a linear solve"},{"category":"section","location":"methods/#Choosing-a-globalization","page":"Solver methods","text":"Line search (ls_). Takes the Newton direction and backtracks the step length until the residual decreases (ls_maxiter, ls_rho). Cheap per iteration; can struggle far from a solution.\nTrust region (tr_). Restricts the step to a region where the linear model is trusted, expanding or shrinking the radius based on how well the model predicted the actual reduction. More robust far from the solution. :tr_direct uses a dogleg step; :tr_iterative uses a hookstep (trust-region-constrained GMRES). Relevant options: tr_radius_init, tr_radius_max, min_step, NR_lim, α, eta.","title":"Choosing a globalization"},{"category":"section","location":"methods/#Rule-of-thumb","page":"Solver methods","text":"Small system, want simplicity: :ls_direct or :tr_direct.\nLarge system (matrix-free): :ls_iterative or :tr_iterative.\nPoor initial guess / robustness needed: prefer a tr_ method.","title":"Rule of thumb"},{"category":"section","location":"methods/#Convergence-and-return-value","page":"Solver methods","text":"A search stops when the residual norm drops below e_norm_tol, when the step norm drops below dz_norm_tol, when the trust-region step falls below min_step, when a callback returns true, or when maxiter is reached. The trust-region and hookstep methods return a status Symbol (:converged, :maxiter_reached, :min_step_reached, :callback_satisfied); the line-search method returns nothing. In all cases the orbit z is refined in place.\n\nSee Options for the full list of tunable parameters.","title":"Convergence and return value"},{"category":"section","location":"#NKSearch.jl","page":"Home","text":"A Newton–Krylov solver for finding periodic orbits and relative periodic orbits of dynamical systems, using a multiple-shooting formulation.\n\nGiven a system ẋ = f(x) and an approximate guess for a closed orbit, NKSearch refines the guess until it satisfies the periodicity condition to a chosen tolerance, while simultaneously correcting the period T and, for a relative periodic orbit, a spatial shift s.","title":"NKSearch.jl"},{"category":"section","location":"#What-it-solves","page":"Home","text":"A periodic orbit satisfies\n\nG(x_0 T) = x_0\n\nwhere G(cdot T) is the flow map that integrates the dynamics for a time T. A relative periodic orbit closes only up to a continuous spatial symmetry,\n\nS(G(x_0 T) s) = x_0\n\nwith S the shift operator. The orbit point x_0 and the scalars T (and s) are all unknowns, solved together by a globalized Newton iteration. The Jacobian can be assembled and factorised directly, or applied matrix-free and inverted with GMRES — the latter scales to the large states typical of discretised PDEs.","title":"What it solves"},{"category":"section","location":"#When-should-I-use-this?","page":"Home","text":"You have a time integrator and want to converge a periodic (or relative periodic) orbit from an approximate guess.\nThe state may be large (e.g. a discretised PDE), so a matrix-free option is attractive.\nYou want multiple shooting for robustness on long or sensitive orbits.","title":"When should I use this?"},{"category":"section","location":"#Installation","page":"Home","text":"NKSearch and its solver dependencies are not in the General registry; install them from the Davide-Lasagna-s-Lab organisation:\n\nusing Pkg\nPkg.add(url=\"https://github.com/Davide-Lasagna-s-Lab/Flows.jl\")\nPkg.add(url=\"https://github.com/Davide-Lasagna-s-Lab/GMRES.jl\")\nPkg.add(url=\"https://github.com/Davide-Lasagna-s-Lab/NKSearch.jl\")","title":"Installation"},{"category":"section","location":"#Where-to-next","page":"Home","text":"Concepts — multiple shooting, the MVector unknown, phase conditions, and the operator interface you provide.\nTutorial — a complete, runnable example converging a limit cycle.\nSolver methods — choosing among the four method options.\nAPI reference — the exported types and functions.","title":"Where to next"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"This tutorial converges a known limit cycle from a deliberately wrong guess. It uses the Viswanath (2001) system, whose limit cycle is the unit circle with period 2pi:\n\ndot x = -y + mu x (1 - r) qquad\ndot y =  x + mu y (1 - r) qquad\nr = sqrtx^2 + y^2\n\nThe code blocks below run during the documentation build, so they are known to work with the current API. They require the Flows and GMRES packages (see Installation).","title":"Tutorial"},{"category":"section","location":"tutorial/#1.-Define-the-dynamics","page":"Tutorial","text":"search! needs the right-hand side F and, for the linearised flow, the Jacobian of the right-hand side. We follow the Flows calling conventions: F(t, x, dxdt) for the system and Fjac(t, x, dxdt, v, dvdt) for its linearisation.\n\nusing NKSearch, Flows, LinearAlgebra\n\nstruct System\n    μ::Float64\nend\n\nfunction (s::System)(t, u, dudt)\n    x, y, μ = u[1], u[2], s.μ\n    r = sqrt(x^2 + y^2)\n    dudt[1] = -y + μ*x*(1 - r)\n    dudt[2] =  x + μ*y*(1 - r)\n    return dudt\nend\n\nstruct SystemLinear\n    μ::Float64\n    J::Matrix{Float64}\nend\nSystemLinear(μ) = SystemLinear(μ, zeros(2, 2))\n\nfunction (s::SystemLinear)(t, u, dudt, v, dvdt)\n    x, y, μ = u[1], u[2], s.μ\n    r = sqrt(x^2 + y^2)\n    s.J[1,1] = μ*(1 - r - x^2/r); s.J[1,2] = -1 - μ*x*y/r\n    s.J[2,1] =  1 - μ*x*y/r;      s.J[2,2] = μ*(1 - r - y^2/r)\n    return mul!(dvdt, s.J, v)\nend\n\nμ = 1.0\nF    = System(μ)\nFjac = SystemLinear(μ)\nnothing # hide","title":"1. Define the dynamics"},{"category":"section","location":"tutorial/#2.-Build-the-flow-operators","page":"Tutorial","text":"G integrates the nonlinear system; L integrates the base state and a perturbation together (couple). Here we use a fixed-step RK4 integrator.\n\nG = flow(F,\n         RK4(zeros(2), Flows.NormalMode()),\n         TimeStepConstant(1e-3))\n\nL = flow(couple(F, Fjac),\n         RK4(couple(zeros(2), zeros(2)), Flows.NormalMode()),\n         TimeStepConstant(1e-3))\nnothing # hide\n\nIf you did not have Fjac, you could instead use a matrix-free linearisation L = JFOp(G, zeros(2)).","title":"2. Build the flow operators"},{"category":"section","location":"tutorial/#3.-Make-an-initial-guess","page":"Tutorial","text":"We use two shooting segments. The seeds are placed at radius 2 (the true orbit has radius 1), and the period guess is 2pi.\n\nz = MVector(([2.0, 0.0], [-2.0, 0.0]), 2π)\nnsegments(z), z.d","title":"3. Make an initial guess"},{"category":"section","location":"tutorial/#4.-Search","page":"Tutorial","text":"F is wrapped to the F(out, x) convention that search! expects. We use the hookstep trust-region method with a matrix-free GMRES solve.\n\nstatus = search!(G, L, (dudt, u) -> F(0, u, dudt), z,\n                 Options(method      = :tr_iterative,\n                         maxiter     = 25,\n                         e_norm_tol  = 1e-12,\n                         gmres_maxiter = 5,\n                         verbose       = false,\n                         gmres_verbose = false))\nstatus","title":"4. Search"},{"category":"section","location":"tutorial/#5.-Check-the-result","page":"Tutorial","text":"z has been refined in place. The seed radii should be 1 and the period 2pi:\n\n(radii = map(norm, z.x), period = z.d[1])\n\n@assert maximum(abs, map(norm, z.x) .- 1) < 1e-8\n@assert abs(z.d[1] - 2π) < 1e-8\nprintln(\"converged to the unit limit cycle\")","title":"5. Check the result"},{"category":"section","location":"tutorial/#Where-to-go-next","page":"Tutorial","text":"Try other Solver methods by changing method (for a 2×2 system the :ls_direct and :tr_direct methods are fine and run single-threaded).\nFor a relative periodic orbit, build the guess with a shift, MVector((x1, x2), T, s), and call the six-argument search!(G, L, S, F, dS, z, opts).\nSave and reload converged orbits with save_seeds / load_seeds!.","title":"Where to go next"}]
}
